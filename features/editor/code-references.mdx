---
title: "Code references in chat"
description: "Cite exact file paths and line ranges with /code inside Clarua chat, so the agent updates the specific lines you mean, not something else nearby."
icon: "code"
---

Use `/code` in chat to reference code with **line ranges**, so you cite exactly the lines you mean.

## Follow-ups see the whole project

A setting lets the model see the project on follow-up turns, not just the current message, so revisions land in the right places.