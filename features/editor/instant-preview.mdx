---
title: "Instant preview"
description: "Single-page apps in Clarua editor preview the moment you change them, with no server round-trip or full rebuild, so edit and result stay in tight sync."
icon: "bolt"
---

Single-page apps preview the moment you change them.

## No build wait

The view is bundled in the browser, so there's no server round-trip and no build step between an edit and seeing it. The preview is effectively instant, even from a cold start.

## Per-project toggle

Turn the live preview on or off per project from the project settings.